node_modules/
dist/
out/
.next/
.next-*/
.tmp/
.DS_Store
*.log
/OpenDesign.exe
.vite
.astro/
.vscode

# Local runtime data — auto-created by the daemon on first start.
# Holds app.sqlite (project metadata), projects/<id>/ (per-project artifacts,
# the agent's CWD), and artifacts/ (one-off renders). Never commit.
.od
.od-e2e
test-results
playwright-report
e2e/.od-data
e2e/playwright-report
e2e/reports/html
e2e/reports/playwright-html-report
e2e/reports/test-results
e2e/reports/results.json
e2e/reports/junit.xml
e2e/reports/latest.md
e2e/ui/.od-data
e2e/ui/reports
e2e/ui/test-results
apps/web/playwright/

# Legacy folder name from before the rename; keep ignored so existing
# clones don't accidentally stage stale runtime data.
.ocd

tsconfig.tsbuildinfo

.claude-sessions/*

.cursor/
.agents/
.opencode/
.claude/
.codex/
.deepseek/

# Commander task scratchpad; keep local task notes out of git by default.
.task/
task.md
specs/change/active
.ralph/

# Local design assistant context
.impeccable.md

# Personal files — keep local, never commit
CV Pablo Meneses*.html
CV Pablo Meneses*.md
edited_image.png
od-*.png
wsc-lat-live.png

# Client proposals & documents (local only)
arza-plan-trabajo.*
carta-nous-research.*
propuesta-*.*
para-nous/
recuperados/

# Duplicate/sandbox directories
WeLaw/

# Other agent configs (not part of OD+OpenCode flow)
.kimi/

# Vendor: prevent accidental commits of generated/build files
vendor/gstack/node_modules/
vendor/gstack/browse/dist/
